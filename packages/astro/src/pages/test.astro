---
import Layout from '../layouts/Layout.astro'
import { getEntryBySlug } from 'astro:content'
const entry = await getEntryBySlug('blog', 'test')
const { Content } = await entry.render()
---

<Layout title="Test">
  <main>
    <Content />
  </main>
</Layout>
